# Real deployment record for quinmini.leanflag.net
# Unknown security-sensitive fields left as __FILL_ME__ (user-owned).

DEPLOY_NAME=quinmini
# pending until ALLOW_CIDRS and UPSTREAM_HOST are filled; validate rejects active+empty
DEPLOY_STATUS=pending

HOSTNAME=quinmini.leanflag.net
CF_ZONE=leanflag.net

# 1Password path for the Cloudflare API token — unknown; do not invent
CF_TOKEN_REF=op://__FILL_ME__/__FILL_ME__/credential

CF_PROXIED=true
ORIGIN_IP=195.85.88.187

# Dev server internal IP running mini-tunnel — unknown; do not invent
UPSTREAM_HOST=__FILL_ME__
UPSTREAM_PORT=3000

OPENRESTY_HOST=local-sbase-dev-openresty
SSL_CERT=leanflag.net

AUTH_MODE=oauth2

# Office/VPN CIDRs for the allow/deny gate — unknown; do not invent
ALLOW_CIDRS=__FILL_ME__
